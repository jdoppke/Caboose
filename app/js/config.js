var CONFIG = {

    // The URL to connect to, first entered when starting app.
    url: null,

    // Whether or not to time represented in the UI should be synced
    // to the server or the local computer.
    timeSync: null,

    // If time is synced to the server, this will be what that time
    // offset is.
    timeOffset: 0
};